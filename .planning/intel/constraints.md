# Ingested Constraints

- Preserve current CLI commands while refactoring internal code structure
- Do not let Codex mutate Jira governance fields such as Sprint, Priority, Assignee, or Fix Version
- Real execution requires valid local repo paths, module paths, and runnable test commands
- Continue using Python 3, argparse, urllib, JSON schemas, Codex CLI, and Jira REST API as the core stack for this milestone
