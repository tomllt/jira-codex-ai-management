# Ingested Context

- Project type: brownfield Python CLI prototype for Jira + Codex collaboration
- Primary user/system boundary: Jira for workflow governance and audit, Codex CLI for analysis/execution, orchestrator for coordination
- Existing repo state: docs + schemas + prompts + examples + `scripts/` implementation
- Verified environment: local Jira Server, real `codex exec` invocation, and task context preflight checks
- Current gap: engineering structure, tests, field governance, and continuous operations lag behind prototype feature growth
